document.addEventListener('DOMContentLoaded', () => {
    console.log('Challenge CMD chargé.');
});

// JS minimal pour éventuels comportements basiques (optionnel)
console.log('CTF — Injection de commandes (CMD) — interface chargée');